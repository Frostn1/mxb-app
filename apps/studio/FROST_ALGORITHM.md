# Frost algorithm versions

The version stamped into every generated track (`<slug>/frost-algorithm.ini`, and the README).
Minor for a new feature, patch for a fix, 0.x until the generator is finished. Bump
`FROST_ALGORITHM_VERSION` in `tracksynth.rs` and add a row here with every generator change.

| Version | Date | Commit | Change |
|---|---|---|---|
| 0.43.2 | 2026-09-17 | claude/supercross-rhythm-coordinates-73dwgk | Every hill in a rhythm lane has a lip, and a tuff block is one colour. A drawn shape's take-off profile — the curve at the foot and the straight run to the lip — was applied to the run into its *first* crest and to no other, and the crests after it were left to the interpolating cubic, which arrives at a turning point flat. So a rhythm lane of five or six hills was built as one lip and four waves, and a triple got a lip onto its first crest and a rounded knuckle onto its far one; the pass that keeps the blend smoothing off a lip protected the first one too. Measured on Hollowbrook Park, seed 7, hills two to six of a six-hill lane: the last metre into each drawn lip stood at 12–15° against the 29° the shape asks for, and stands at 25–29° now. The first hill, which always had its lip, is unchanged at 18°. Ridden, that is "the rhythm doesn't have much of a lip, I G out every time and get sent to the moon": with nothing to leave the ground over, a rider rolls the crest, drops into the trough compressed, and the next face fires them off whatever the suspension gives back. Separately, every face of a tuff block was mapped across the whole sheet, so each block wore the red half *and* the white half split down its middle with the grime banded along its top, and a lap's worth of them read from the seat as popcorn. A block now takes one half of the sheet, alternating red and white down the row, the grime is at its foot where the dirt is, and the grain is finer and fainter than the centimetre-wide blotches that survived every mip. And every piece of a lane border — blocks and banner alike — is seated on the lowest ground its own footprint covers rather than on the height under its middle: a rigid box on the flank of a jump's mound stood with daylight under one end, measured at 16% of a lap's feet lifted and 0.24 m at the worst. Motocross is untouched by all of it: its walker draws no hand-drawn shapes and lines no lane borders |
| 0.43.1 | 2026-09-17 | fix/studio-track-from-place | A scanned track keeps the credit its survey asks for. The ground's attribution travels from the lap file into the programme and out into `location` and `<slug>/place.txt`, where before it stopped at the lap file and only the licence wording went on. `location` is cut to 150 characters a whole piece at a time, source first and credit second, because the game lists a track whose location runs to 155 and does not list one at 193, and the old 48-character cut at the first semicolon dropped the credit every time. The picture under the track's name keeps the short label. Nothing moves for a generated track: Northgate seed 103 builds a byte-identical .trh, 0 of 4,198,401 cells moved, and an identical source tree bar the two files that carry this version string |
| 0.43.0 | 2026-09-16 | feature/lidar-pkz-packaging | A scanned venue is painted in the tones the photograph actually has, and the archive is one the game will open. The photograph is registered to the plot by world coordinates instead of being stretched across it — the tile is 500 m, the plot a 470 m window inside it, and the paint was up to 15 m from the ground it described, right in the middle and furthest out at the edges. Its soil is then banded by its own luminance quantiles rather than sorted into dirt-or-grass, so the dark worked ground, the ordinary dirt, the dry pale field and the hardstanding each land on their own sheet: three of the seven ground layers used to be empty and one pale sheet tiled 470 m of venue. The classes reach the masks as coverage rather than as a yes-or-no, so a boundary is a boundary and not a staircase of four-texel blocks. Nothing a rider can see still comes from the centreline: the corridor masks stay empty and two completely different laps over the same tile build the same ground. A track's `location` is a label again — the provenance note it had swallowed ships as `<slug>/place.txt` inside the archive, where a licence can actually travel. And every track the app builds now leaves through `trackbuild::finish`, so there is one packer: a `.pkz` written by the `zip` command carries a 28-byte extra field in every local header that the game's reader does not allow for, which is why four scanned tracks were perfect inside and listed nowhere. Nothing moves for a generated track: Northgate seed 103 builds a byte-identical .trh, 0 of 4,198,401 cells moved |
| 0.42.0 | 2026-09-16 | feature/lidar-track-import | A track can be built on the ground of a real place. A public-domain lidar tile and a lap traced over it are read straight into the generator: the heightfield stops being invented and becomes the piece of land the circuit was actually cut into, with its banks, its ravine and the jumps that were standing on it the day it was flown. The traced polyline is fitted to the straights and arcs the track format is made of by walking it — each segment starts exactly where the last one ended, so the chain is continuous by construction rather than stitched afterwards — and what the fit cost is measured and reported rather than assumed. Scanned ground keeps its own steep places: the pass that slumps walls flat is for an artefact the generator makes and real ground does not have, and on the Ironman plot it would have flowed away the 0.95% of the site that is the ravine bank. The height budget is taken from the ground's own relief instead of the 22 m default, which Ironman's 23.5 m would have clipped. Flight-line striping is filtered, mildly and honestly: the tile has a measured directional grain and this removes the part of it that is coherent along the flight line, about a tenth of the excess, for 0.9 cm rms. Nothing moves for a generated track: Northgate seed 103 builds a byte-identical .trh, 0 of 4,198,401 cells moved, and an identical source tree of 82 files |
| 0.41.0 | 2026-09-16 | feature/track-lane-borders | Two choices a stadium lap did not have. What lines a lane's borders is now soft tuff blocks, solid ones, a printed banner wall or nothing, where before it was the two kinds of block; the banner is the lap's own hoarding atlas cut to a lane's height and run down the border line, one sponsor's design for about thirty-seven metres at a time, in three models so a long lap stays under the 65,535 vertices a draw group may index. And whether a supercross lap stands in a stadium at all is a choice: left alone it builds the bowl it always did, and set to open air it gets the outdoor venue the other two disciplines get — trees, the wood behind them, the backdrop bank and the sponsor wall — with one ring of site fence laid where the stadium's wall stood, so the lap is still bounded. Both are left out of a project file at their defaults, so a project saved before this reads the same; the old `tuff` key is still read. Nothing moves at the defaults: Northgate seed 103 builds a byte-identical `.trh` and an identical source tree bar the two files that carry this version string, and twelve SX and SMX laps build identical scenery, model for model |
| 0.40.0 | 2026-09-16 | feature/sx-brief-disciplines | A written brief can ask for supercross. The discipline is a setting the model picks from the brief rather than the switch on screen alone, so "a supercross round in a stadium" builds a stadium lap and "an SMX playoff round" builds a SuperMotocross one; the switch still wins whenever it is set to something. A brief that names a stadium discipline is always asked for the character rather than for a whole lap, because only the stadium walker builds one. The brief's supercross words reach the lap: no whoops asked for means the set the seed rolled is not built, and sand asked for is laid rather than left to the corpus's coin flip. A brief that asks for nothing in particular draws exactly what it drew before, and motocross is untouched — it never enters the section-built path |
| 0.39.0 | 2026-09-16 | feature/studio-texture-picker | What a track looks like is no longer what it is made of. `terrain.surface` still decides the ride — the material stack the game deforms, the shoulder's surface id, how wide that shoulder runs — and a new `terrain.texture` decides the paint on its own: a look to start from (`ride`, `soil`, `sand`, `grass`, `stadium`) plus up to four of the rider's own images, one for the riding surface, one for the line, one for the ruts and one for whatever lies beyond. Stadium is composed out of the soil bands rather than graded to a palette of its own — the dark worked soil on the lanes, the dry soil on the floor outside them, and no 3D grass — because the ground photographs are a published track's and are not ours to re-tone; new looks arrive with art of our own. An imported image is decoded, cropped square, resampled to 1024 and copied into the Studio's own folder under the hash of its own pixels, so a project builds after the file it came from has gone and an id nothing is stored under paints with the built-in it stood in for. Left alone, `texture` is not written to the file and not read: Northgate seed 103 builds the same `.trh` and the same 82 source files, byte for byte, as it did on 0.38.0 |
| 0.38.0 | 2026-09-16 | feature/track-density | The ground comes back to grade between hills. A run of hills was drawn with its troughs a fourteenth of the way up the hill and then dug a hollow at each end like a single jump, so the whole run stood proud of the floor beside it: measured, our troughs sat 0.28 m above that floor where all seven rounds sit at 0.01-0.03, and 44% of them never came back under the height a jump is counted off, so neighbouring hills merged into one long stretch. Troughs now go to grade and a drawn run is not dug a hollow. Hills are drawn at the height the corpus measures (1.25-1.60, median 1.44) rather than at 0.9-1.5 measured off a floor the hollow had lowered, which a rider felt as 0.96 m crest to trough against a real 1.43. Built: troughs 0.28 to 0.18 m, troughs not clearing 0.30 m from 44% to 15%, crest to trough 1.14 to 1.37 m, and density 45.6 to 57.4 features a kilometre, inside the measured 56.8-71.9 |
| 0.37.0 | 2026-09-16 | feature/track-density | SX and SMX laps are packed to the density the corpus carries: a section was capped at its band and then centred in its lane, so a 110 m lane held 65 m of jumps and 45 m of nothing — over twelve seeds 55% of the lap stood bare and it measured 42 lips a kilometre against the seven rounds' 64. Whatever room a lane has left over now takes another section, biggest hole first, until the lap carries what a real round carries; nothing already laid is moved or resized, so pitch, hill height, whoop spacing and lip angle stay where they measured. How packed it is is a slider in Track Studio, from sparse through the measured density to packed, and a brief's `jumpDensity` reaches the same knob. Motocross is untouched — it never enters the lane-built path |
| 0.36.0 | 2026-09-15 | feature/track-sx-sections | SX and SMX laps are built out of sections rather than a roll per feature: rhythm lanes of 5–6 hills at a 10 m pitch, one set of 9–10 whoops out of a 180 in the second half of the lap, triples sized by what the run-up actually carries, standalone doubles, tabletops, and a sand stretch carrying both its painted band and a half-metre `sand` material layer; the lap finishes over a triple rather than a tabletop; a jump's side mound is capped by the ground there really is, so a rhythm hill no longer stands a metre deep on the lane beside it; whoops and rhythm lanes keep the height they were drawn at instead of losing a third of it to the blend; SX builds a stadium — a wall ring and tiered stands, no trees, wood, backdrop or paddock; SMX walks 180s, one long side up to 350 m and stadium-style blocks; tuff blocks line the lane borders, soft (pass-through) or solid per track; a whoop set stands on the ground beside it instead of on a pad dug by the jumps either end of it, so its crests read the height they were drawn at; the sand band is exported only where a lap actually carries sand; the finish jump clears the ground its own footprint covers rather than a tabletop's; and motocross draws and builds exactly what it drew and built before — the mound cap and the drawn-shape restore are both for disciplines whose lanes abut |
| 0.35.0 | 2026-09-15 | feature/track-discipline | MX, SX and SMX: a rules table per discipline (plot, lap, width, start, finish jump, gates, review bands); SX is a new stadium walker — lanes across a flat floor, each ending where its run puts it, so a lane either turns back (a 180) or steps across (two 90s), with a return lane and a 22-gate start; SMX walks the outdoor lap flatter and tighter; MX draws exactly what it drew before |
| 0.34.0 | 2026-09-15 | feature/byo-llm-trackgen | the random lap takes settings — lap length, width, corners, jumps, surface, wear, ARL roughness, hills — so a model can pick them from a brief; with none given it draws exactly what it drew before |
| 0.33.0 | 2026-09-15 | feature/random-track-scales | a random track picks a scale: Easy (jumps at 70% tall, tables and gaps 70% long; wear 0.25, roughness 0.6 for shallower ruts), Normal (as drawn), or ARL (the `FROST_ROUGH=2` raced build: roughness 2, jumps grown ×1.35) |
| 0.32.0 | 2026-09-14 | feature/table-mounds | jumps spill wider the taller they are: sides round over the top, stand at most 28° a third of the way down and run out in a long skirt (a 3 m table stood at 38° within 6 m); rounded along the lap past the track and stopped 2 m short of another leg |
| 0.31.2 | 2026-09-13 | fix/arl-triples-75 | ARL triples at 75% and tabletops and doubles at 80% of the full ARL growth (triples were 90%) |
| 0.31.1 | 2026-09-13 | 590e8c87 (#603) | 35 cm soft soil over the whole plot as ARL's pro tracks carry, on a 40 cm soil bed (was 0.146 m in all); no longer thinned by wear |
| 0.31.0 | 2026-09-13 | 977f073c (#602) | Northgate layout pinned; ruts in every real corner (48/26 m); ARL lanes vary down the lap; ARL braking sets keep a gap and lone bumps sit before them; ARL triples 90%; side singles slope, not wall, and keep their size on ARL; scenery fixes |
| 0.30.0 | 2026-09-13 | 5861c3cd (#598) | laps drawn with at most 55 m between corners (70 m on the way home) and no chicanes; the start straight runs into turn one |
| 0.29.1 | 2026-09-13 | f86f423b (#598) | singles 50 m apart, rollers 0.9-1.2 m so chicanes aren't empty, ARL singles taller not longer, step-ups no longer clear rollers on ARL |
| 0.29.0 | 2026-09-13 | cc20a1b3 (#596) | spawns in the paddock, painted pit road to the gates, whole team rigs and pop-ups, sparser turn markers |
| 0.28.0 | 2026-09-13 | 44824b98 | bigger ARL jumps, ruts over jumps, more braking bumps |
| 0.27.1 | 2026-09-13 | e2e2baee | fill the straights before the finish, shorter taller step-ups |
| 0.27.0 | 2026-09-13 | fa4632ce | paddock with team areas, pit road, start-gate sponsor wall |
| 0.26.0 | 2026-09-13 | 8c61f159 | taller jumps, full-width braking bumps, turn markers, fenced start |
| 0.25.0 | 2026-09-13 | e1a37319 | straight-line landings, pit area, ARL bumps and jumps, arches that draw |
| 0.24.1 | 2026-09-13 | 82eb6017 | bare sweepers, singles before turns, landings clear of turns, ARL bumps |
| 0.24.0 | 2026-09-12 | 785beee1 | rider round 9 - face ruts on corner lanes, no trench, short straights, roughness knob |
| 0.23.2 | 2026-09-12 | 11c66de4 | plant only library trees that show on their sheet |
| 0.23.1 | 2026-09-12 | 10607eca | track picture frames the tallest jump, less haze, mipped models |
| 0.23.0 | 2026-09-12 | bcabe25e | track picture is a close-up of the finish with scenery and title |
| 0.22.1 | 2026-09-12 | c92b2a86 | real doubles, steeper tabletops, ARL roughness, banners off the track |
| 0.22.0 | 2026-09-12 | 27b79d06 | air take-offs for doubles, triples and singles |
| 0.21.0 | 2026-09-12 | 4cb6f0ee | draw the track-info map as an overhead of the real ground |
| 0.20.5 | 2026-09-12 | d4429588 | lift donor props relative to the donor's ground and drape them |
| 0.20.4 | 2026-09-12 | 19e511a1 | arches on two legs, even lead-in ruts, steeper take-offs |
| 0.20.3 | 2026-09-12 | be1cc1f7 | place donor arches across the track instead of removing them |
| 0.20.2 | 2026-09-12 | 1c2f9971 | lead-in ruts bend into corners, face ruts run through the lip |
| 0.20.1 | 2026-09-12 | 602fa9a9 | keep big donor structures away from the track |
| 0.20.0 | 2026-09-12 | fddfbd03 | lead-in ruts, ground-level track, side singles, banner fix |
| 0.19.0 | 2026-09-12 | 9ed7e4b9 | bigger jumps, wet main lines, sharper tyre marks, map fix |
| 0.18.3 | 2026-09-12 | 4a2ef954 | braking bumps that roll, and start ruts as a comb |
| 0.18.2 | 2026-09-12 | 2a753e9a | tyre lines back, a start straight worked like track, ride fixes |
| 0.18.1 | 2026-09-12 | db3b0663 | a start straight that narrows, merges smoothly and looks like track |
| 0.18.0 | 2026-09-12 | bd734e03 | real objects only, finish arch on the jump, more ruts and jump types |
| 0.17.0 | 2026-09-12 | b4d7179d | light generated tracks like published ones, and drop tear-offs |
| 0.16.1 | 2026-09-11 | c48ef3ac | lifted scenery wears its own sheets, trees see-through |
| 0.16.0 | 2026-09-11 | 4e14b90e | a backdrop beyond the plot, a thicker wood, scenery at published distances |
| 0.15.3 | 2026-09-11 | 93af1115 | pass over drawn laps whose start straight lands on the lap |
| 0.15.2 | 2026-09-11 | 94e12c13 | scenery with its own textures, ruts that hand over, a touch more lip |
| 0.15.1 | 2026-09-11 | 7e448c0e | braking bumps as tall as Indiana's |
| 0.15.0 | 2026-09-11 | 5eef5bce | each braking bump its own size and shape |
| 0.14.0 | 2026-09-11 | 65462e2a | braking bumps form in staggered lines, not ridges across the track |
| 0.13.0 | 2026-09-11 | 0e582d8e | pebbles, a finer riding-surface mask, 70% jumps, bumps you can feel |
| 0.12.0 | 2026-09-11 | 4be907e8 | bake the ground's own relief into the riding surface's normal map |
| 0.11.0 | 2026-09-11 | ecbf440f | rut lanes that wander, come and go, and vary in width |
| 0.10.0 | 2026-09-11 | c657623a | braking bumps in sets of long waves, clear of the ruts, painted |
| 0.9.0 | 2026-09-11 | adc70206 | tight corners carry stated rut lanes |
| 0.8.0 | 2026-09-11 | 0175d452 | taller jumps with gentler faces, ruts that run through turns, darker dirt |
| 0.7.1 | 2026-09-11 | 33b089ec | keep Indiana's specular with its normal maps |
| 0.7.0 | 2026-09-11 | 2ac7c09d | photographed ground takes Indiana's own normal maps |
| 0.6.0 | 2026-09-11 | a553ff0e | more ruts per corner, and ground that catches the light |
| 0.5.1 | 2026-09-11 | 197f8224 | take-offs curve up then run straight to the lip |
| 0.5.0 | 2026-09-11 | de7ab023 | a longer, gentler finish face; ruts across more of a corner; braking bumps |
| 0.4.0 | 2026-09-10 | 9c2ec959 | takeoffs concave to the lip and flush with the deck; longer finish deck |
| 0.3.0 | 2026-09-10 | 4c75320a | jumps at 85% of their old height, the finish jump at full size |
| 0.2.4 | 2026-09-10 | 06e7b482 | scuff only the takeoff, and ease a jump's hold on the ruts in |
| 0.2.3 | 2026-09-10 | 8a7e9e35 | ease the jump-face paint in, and widen the tyre lines |
| 0.2.2 | 2026-09-10 | 663ac80e | paint the light dirt on the track only, and blend the ruts |
| 0.2.1 | 2026-09-10 | 8a65b310 | cap rut depth and thin the ruts in a corner |
| 0.2.0 | 2026-09-10 | 38ae2ce3 | middle-ground ruts, smaller jumps with a kick, darker riding surface |
| 0.1.2 | 2026-09-10 | 63cf9cac | the ground picker changes the ground, not the track |
| 0.1.1 | 2026-09-10 | cd63d835 | take the chop and the rut height out of generated ground |
| 0.1.0 | 2026-09-10 | 4c91ed95 | stamp the track generator version into built tracks |
