node_modules
.wrangler
# Generated by `wrangler types` from wrangler.jsonc — regenerate rather than commit drift.
worker-configuration.d.ts
.dev.vars
# Invite codes are credentials and this repository is public — keep the filled-in seed out.
# `seed.example.sql` is the template.
seed.sql
