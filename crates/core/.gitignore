# Proprietary, local-only, placed by the private repo's sync.sh. The old
# src-tauri/.gitignore only covered these at their pre-monorepo paths, and a
# `git add -A` put them in a public repo once already.
/src/sidecar.rs
/src/sidecar_lock.rs
/src/mxbsecure.rs
/src/fixtures/test_sidecar.pkz
/src/mxbsecure.dll
