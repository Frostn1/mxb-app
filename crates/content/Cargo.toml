[package]
name = "mxb-content"
version = "0.1.0"
description = "Headless readers for MX Bikes content containers and terrain descriptors."
authors = ["Frost"]
edition = "2021"
publish = false

[dependencies]
anyhow = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }

[dev-dependencies]
tempfile = "3"
